function solve(a, b, c) {
    // TODO: use nested conditionals
}

function hackySolve(a, b, c) {
    let largest = Math.max(a, b, c);

    console.log(`The largest number is ${largest}.`);
}

hackySolve(5, -3, 16);
